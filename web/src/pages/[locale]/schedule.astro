---
import Main from '../../layouts/main.astro';
import ScheduleSection from '../../components/schedule/ScheduleSection.astro';
import ModalShell from '../../components/modals/ModalShell.astro';
import { localeStaticPaths, resolvePageLang, type Locale } from '../../i18n/locales';

export function getStaticPaths() {
	return localeStaticPaths();
}

const locale = Astro.params.locale as Locale;
const lang = resolvePageLang(locale);
if (!lang) return Astro.redirect('/sk/');
---

<Main title="Rozvrh — Zé Radiola Team" currentPath="schedule" lang={lang} locale={locale}>
	<div class="mx-auto max-w-[1240px] px-10 py-16">
		<ScheduleSection lang={lang} kickerKey="schedKicker" titleKey="schedTitle" titleSize="xl" />
	</div>

	<ModalShell slot="modals" />
</Main>
